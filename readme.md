para visualizar lo que tenemos que descargar y en que version.
pip freeze > requirements.txt

para instalar las librerias que necesitas sin necesidad de poner una por una(estas estan en un archivo llamado requirements.txt) 
pip install -r requirements.txt